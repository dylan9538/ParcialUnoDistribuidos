worker_processes  1;
events {
   worker_connections 1024;
}

http {
    upstream servers {
         server <%=@ip_serverOne%>;
         server <%=@ip_serverTwo%>;
    }
    server {
        listen 8080;

        location / {
              proxy_pass http://servers;
        }
    }
}
